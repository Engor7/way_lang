# База — файл SQLite на диске. Значение по умолчанию (data/way-lang.db)
# подходит почти всегда: файл создаётся сам при первом запуске, миграции
# применяются на старте. .env.local нужен, только чтобы положить базу
# в другое место.
# DATABASE_FILE=data/way-lang.db
